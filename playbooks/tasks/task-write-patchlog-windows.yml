# - name: Write Windows patch log
#   lineinfile:
#     create: yes
#     path: "/mnt/protokolle/{{ current_year }}/{{ current_month }}/Serverpatch/Fertig/Updates-{{ inventory_hostname }}-Generated.txt"
#     line: |
#       ###########################################################################################################################

#       {{ inventory_hostname }} ({{ ansible_facts.os_name }}):
#       Updates({{ update_result.found_update_count }}):
#       {% for update in update_result.updates %}
#       - {{ update_result.updates[update].title }}
#       {% endfor %}
      
#       Done: Neustart von {{ reboot_start.stdout }} bis {{ reboot_end.stdout }} = OK
#       ToDo: Festplattenspeicher {{ disk_space_gb }} GB =
#       {{ windows_verson_check }}
#       ToDo: WSUS Status prüfen =
#       ToDo: Logs > Anwendung = 
#       ToDo: Logs > System = 
#       ToDo: Abmelden = 
#   delegate_to: localhost
- name: Write Windows patch log
  local_action:
    module: copy
    content: |
      ###########################################################################################################################

      {{ inventory_hostname }} ({{ ansible_facts.os_name }}):
      Updates({{ update_result.found_update_count }}):
      {% for update in update_result.updates %}
      - {{ update_result.updates[update].title }}
      {% endfor %}
      
      Done: Neustart von {{ reboot_start.stdout }} bis {{ reboot_end.stdout }} = OK
      ToDo: Festplattenspeicher {{ disk_space_gb }} GB =
      {{ windows_verson_check }}
      ToDo: WSUS Status prüfen =
      ToDo: Logs > Anwendung = 
      ToDo: Logs > System = 
      ToDo: Abmelden = 
    dest: "/mnt/protokolle/{{ ansible_date_time.year }}/{{ ansible_date_time.month }}/Serverpatch/Fertig/Updates-{{ groupe_name }}-Generated.txt"
  run_once: true
  delegate_to: localhost
